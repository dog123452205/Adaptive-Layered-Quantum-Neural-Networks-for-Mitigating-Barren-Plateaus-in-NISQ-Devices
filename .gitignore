# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Node
node_modules/

# Logs (regenerable from re-running training/scripts)
*.log

# Claude Code local settings (machine-specific, not shared)
.claude/

# Detailed per-run RL results (checkpoints + raw logs, regenerable by re-running training)
outputs/rl_results/

# Thesis report draft (kept local only, not published to the repo)
report/

# Datasets - only publish AI4I (predictive maintenance) and heart on GitHub;
# everything else in data/ stays local only
data/*
!data/heart.csv
!data/prepare_heart.py
!data/predictive_maintenance.csv
!data/ai4i_smote_and_angle_encoding_demo.ipynb

# Old demo package (kept local only, not published to the repo)
al_qnn_demo_package/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
